{"id":"mem-159a09c17bc0b2c6","information":"vp migrate (v0.2.9, --no-interactive --no-hooks) on a Vite 8 + React + Bun project auto-migrates ESLint→Oxlint AND Prettier→Oxfmt even WITHOUT --full: it deletes eslint.config.js and .prettierrc, removes eslint/prettier/typescript-eslint/prettier-plugin-tailwindcss devDeps, rewrites lint/format scripts to \"vp lint .\"/\"vp fmt\", injects huge lint{}+fmt{} blocks into vite.config.ts, and ALSO rewrites /* eslint-disable */ comments in src files (found in theme-provider.tsx). To defer the lint/fmt migration while keeping core Vite+: git checkout -- eslint.config.js .prettierrc and any touched src files, restore lint/format scripts + eslint/prettier devDeps in package.json, strip lint{}/fmt{} from vite.config.ts keeping defineConfig/lazyPlugins from vite-plus, then bun install. Verified working after: build (tsc -b && vp build), eslint, vp dev smoke test all pass. Generated fmt block also contained a cosmetic duplicate \"pnpm-lock.yaml\" ignorePatterns entry.","created_at":"2026-08-21T18:42:29.731Z","tags":"vite-plus,vp-migrate,gotchas,bun,eslint,prettier,deferral"}
{"id":"mem-372d91d7078727f8","information":"In remindit-pwa (Vite+ React19+TS+Tailwind v4), migrating from Vite+ Oxlint/Oxfmt to Biome succeeded on 2026-08-21: `bun add -d @biomejs/biome` installed 2.5.10. package.json script mapping: `lint` -> `biome check .` (was `vp lint .`), `format` -> `biome format --write .` (was `vp fmt \"**/*.{ts,tsx}\"`), and new `lint:ci` -> `biome ci .`. Biome `ci` command EXISTS (runs check without writing, fails on any diff). dev/build/preview/typecheck stay on `vp`. biome.json uses `files.includes` negated globs (v2 has NO `files.ignore`), `javascript.formatter.semicolons: asNeeded` (NOT `formatter.semicolons`), `assist.actions.source.organizeImports: on` for import sort, and `useSortedClasses` linter rule for Tailwind sorting. Do NOT run full codebase reformat during install; another worker handles that.","created_at":"2026-08-21T20:55:24.411Z","tags":"biome,remindit-pwa,migration,linting,formatting,vite-plus"}
{"id":"mem-38a791bd6ac096d4","information":"When removing Payload CMS collections from the hct-themed monorepo: shared/types/payload-types.ts and admin/src/app/(payload)/admin/importMap.js are GENERATED files, so editing them by hand is wrong. Run `bun run generate:types` then `bun run generate:importmap` in the admin workspace; importmap may report 'No new imports found, skipping writing import map' when removals don't add imports, which is fine. Also note Media collection 'kind' radio option value 'projects' must be kept even when the projects collection is deleted, because web /case-studies and 62 media rows depend on it. Lint for the admin workspace: `bun run lint` (0 errors expected; pre-existing warnings in seed/, tests, Copy.tsx are unrelated). Web check: `bun run astro-check`.","created_at":"2026-08-17T03:42:04.538Z","tags":"payload,cms,collection-removal,hct-themed,generate-types,importmap"}
{"id":"mem-7af59f67cda8f012","information":"remindit-pwa Vite+ migration verified clean (branch feat/vite-plus-migration, commit 2557f3e): commit only touches package.json/vite.config.ts/bun.lock; src/ has zero diff vs main and no oxlint/oxfmt comment remnants. Gotchas found during verification: (1) `vp dev` silently falls back to port 5174 if an orphaned dev server holds 5173 - check `lsof -nP -iTCP:5173` and kill stale node processes (cwd confirms ownership) before smoke testing; (2) killing the `bun run dev`/`vp dev` wrapper does NOT kill the underlying vite-plus-core node CLI process - kill that PID directly; (3) `vp build` output is fast (~266ms) but does not typecheck, so `tsc -b && vp build` in the build script is load-bearing.","created_at":"2026-08-21T18:50:39.214Z","tags":"vite-plus,vp-migrate,dev-server,port-conflict,verification,remindit-pwa"}
{"id":"mem-7cdc8d3564b8dfe7","information":"RESEARCH 1/4 - vite+ and vp migrate for remindit-pwa (Vite 8, React 19, bun, Tailwind v4 CSS-first, Shark/Ark UI). vp CLI v0.2.9 global. Researched 2026-08-21 from viteplus.dev docs + CLI help.\n\nWHAT VITE+ IS: VoidZero unified toolchain = global CLI vp plus local package vite-plus. Bundles Vite 8 (Rolldown default), Vitest 4.x re-exported as vite-plus/test*, Oxlint, Oxfmt, tsdown, Vite Task. Manages node runtime (opt out: vp env off), delegates package management to packageManager field in package.json (bun here). Core alias: vite resolves to npm:@voidzero-dev/vite-plus-core via overrides. vite-plus exports only defineConfig, defineProject, lazyPlugins; all other vite imports keep working through the alias.\n\nvp migrate FILES TOUCHED: package.json gets pinned vite-plus devDependency at the concrete CLI version. Standalone bun projects (no workspaces) get concrete specs and no catalogs because bun catalogs need a workspaces root. Keeps/adds a direct vite dependency plus overrides: vite -> npm:@voidzero-dev/vite-plus-core. Bun-specific rule: the core alias is mirrored as a direct vite dep so Bun resolves the peer provider before applying overrides.","created_at":"2026-08-21T18:26:41.224Z","tags":"vite-plus,vp-migrate,migration,vite,bun"}
{"id":"mem-af5ecebc537d19a7","information":"RESEARCH 2/4 - vp migrate exact behavior for remindit-pwa. Scripts rewritten preserving args: dev \"vite\" becomes \"vp dev\"; build \"tsc -b && vite build\" becomes \"tsc -b && vp build\"; preview \"vite preview\" becomes \"vp preview\". eslint to vp lint and prettier to vp fmt only if those optional migrations run. bunx forms preserved (bunx --bun vite build becomes bunx --bun vp build).\n\nvite.config.ts: the defineConfig import from \"vite\" is rewritten to \"vite-plus\" ONLY in config entry files; app source keeps vite imports which resolve through the alias. Tool-specific configs merge into vite.config.ts blocks: lint{}, fmt{}, staged{}, pack{}.\n\ntsconfig: migration runs ts5to6 fixBaseUrl because tsgolint does not support baseUrl. remindit-pwa tsconfig.app.json uses paths WITHOUT baseUrl so this is a no-op.\n\nindex.html is NOT touched. No vitest imports exist in this repo so no source rewrites happen. Optional setup actions: git hook dispatcher (.vite-hooks/pre-commit plus core.hooksPath) is DEFAULT in non-interactive mode, use --no-hooks to skip; AGENTS.md/editor files only via --agent/--editor flags; ESLint/Prettier migration is opt-in; nvmrc conversion not applicable (no .nvmrc present).","created_at":"2026-08-21T18:26:54.269Z","tags":"vite-plus,vp-migrate,migration,vite,bun"}
{"id":"mem-b6ac2ac2a53ab50f","information":"RESEARCH 3/4 - gotchas for remindit-pwa stack under vite+. Post-migration behavior: deps reinstalled once to refresh lockfile (nonzero exit if install fails), warns on Rolldown-incompatible patterns like manualChunks (not used here), oxfmt formats changed files but SKIPS formatting while Prettier still present. Migration is idempotent.\n\nTailwind v4 (@tailwindcss/vite ^4): no open compatibility issues in voidzero-dev/vite-plus GitHub repo; already proven under Vite 8 Rolldown in this project, plugin behavior unchanged. CSS-first tokens in src/index.css untouched by migration.\n\nReact 19 + @vitejs/plugin-react ^6: already working under Vite 8, migrate makes no framework changes. Optional perf: wrap plugins in lazyPlugins(() => [react(), tailwindcss()]) so vp lint/fmt/check skip loading them during metadata-only config loads.\n\nFormatter trap: prettier-plugin-tailwindcss class sorting. Oxfmt has native sortTailwindcss support but had closed diffs vs Prettier on Tailwind v4 @theme/@theme inline colors (oxc issue 25109). Keep Prettier initially if formatting parity matters; cut over to oxfmt deliberately later.\n\nLinter trap: eslint.config.js exempts generated src/components/ui from several rules; Oxlint migration will not translate custom overrides one-to-one. Oxlint covers react-hooks and react-refresh equivalents; JS plugins escape hatch exists. Do ESLint-to-Oxlint cutover as a separate deliberate step.","created_at":"2026-08-21T18:26:59.702Z","tags":"vite-plus,vp-migrate,tailwind-v4,react19,gotchas"}
{"id":"mem-cb0d9ac79bbb37b5","information":"Centralized dotenv in a Bun monorepo (Next/Payload admin + Astro web + Playwright e2e): root .env is NOT auto-loaded by subprocesses. `bunx --bun` disables Bun's .env auto-loading entirely. Next sets NODE_ENV itself so dotenv cannot override it (only affects non-Next contexts like payload CLI). Astro's config file (.mjs) does not read .env automatically, use Vite's loadEnv or feed env through a regeneration step. Pattern that works: each consumer calls dotenv.config({ path: path.resolve(<workspace>, '../.env') }) explicitly; a config.toml→JSON watcher reads root .env and overrides site.baseUrl, chained into dev/build scripts (node scripts/... && astro dev) so the committed generated JSON picks up env values and can be gitignored.","created_at":"2026-08-18T04:19:10.452Z","tags":"dotenv,monorepo,bun,payload,nextjs,astro,playwright,env,config"}
{"id":"mem-d3e5d6f4bc552f93","information":"Swarm coordination pattern that worked for tooling migrations (vite->vite+): 3 sequential cells (research -> migrate -> independent verify) with research findings injected into the migrate worker's shared_context. vp migrate gotchas discovered: it force-applies Oxlint/Oxfmt cutover even WITHOUT --full and rewrites eslint-disable comments in src files OUTSIDE the worker's file scope - revert with git restore and defer lint/format cutover deliberately. Also: swarm_complete consistently fails with \"Missing required parameters: start_time\" schema bug in this environment; coordinators must close cells manually via hive_close after review. sqlite SQLITE_BUSY errors on hive/swarmmail tools are transient - retry once.","created_at":"2026-08-21T18:53:06.386Z","tags":"coordination,vite-plus,vp-migrate,swarm-patterns,migration"}
{"id":"mem-dfab181d0b21cdcc","information":"RESEARCH 4/4 - commands, sequence, rollback for vite+ migration of remindit-pwa.\n\nCOMMANDS: built-ins cannot be overridden. vp dev/build/test always run the built-in tools; vp run <script> (alias vpr) runs package.json scripts. bun run dev/build keep working because scripts remain in package.json. IMPORTANT: vp build does NOT typecheck; vp check typecheck uses tsgolint (TypeScript 7 Go port), NOT tsc. Must keep \"build\": \"tsc -b && vp build\" since tsc -b is the real type gate for this repo (bare tsc --noEmit on files:[] root verifies nothing per AGENTS.md).\n\nRECOMMENDED SEQUENCE: 0) clean git tree, feature branch. 1) vp upgrade (latest CLI migration rules). 2) confirm Vite 8+ (project already ^8). 3) vp migrate --no-interactive (add --no-hooks to skip hook dispatcher; skip agent/editor flags). 4) review diffs: package.json (vite-plus dep, overrides.vite alias, direct vite edge, script rewrites) and vite.config.ts defineConfig import. 5) verify: bun install, bun run build (tsc -b && vp build), bun run dev, then vp check. 6) defer ESLint/Prettier to Oxlint/Oxfmt cutover as separate deliberate step to isolate risk.\n\nROLLBACK: all edits are working-tree files (package.json, bun.lock, vite.config.ts, maybe hooks/AGENTS.md). Rollback = git restore . && git clean -fd on branch, then bun install. Disable hooks via vp hooks disable or restore core.hooksPath. Remove global tool entirely with vp implode. If install fails mid-migrate the command exits nonzero leaving partial changes; git is the safety net.","created_at":"2026-08-21T18:27:13.760Z","tags":"vite-plus,vp-migrate,migration,rollback,bun"}
{"id":"mem-fb846d8de493feaf","information":"In the remindit-app-pwa (Rsbuild + React 19 + Bun) project: the `bun run lint` command referenced in AGENTS.md does NOT exist as an npm script. Use `bunx @biomejs/biome check src/...` (or `biome lint`) instead. Also `@nanostores/logger` v1.1 exports `logger({ Name: store })` (object map), NOT `logStore`. And `@nanostores/persistent` `persistentJSON<T>(name, initial)` handles JSON object/array persistence with no manual encode/decode. `import.meta.env` is not typed by default (no Rsbuild client types in tsconfig), so augment `interface ImportMeta` via `declare global` or use `import.meta.env?.DEV` with a cast. Rsbuild/Rspack does resolve JSON imports from outside `src/` (e.g. `../../docs/samples/*.json`), so seeding stores by importing a sample JSON file works.","created_at":"2026-08-23T05:10:27.705Z","tags":"remindit,rsbuild,nanostores,biome,lint,import.meta.env"}
{"id":"mem-fdb5fb771a2b29fb","information":"Payload CMS + SQLite: when a collection is removed from payload.config.ts, the dev-server auto-sync will NOT drop orphaned tables or their FK columns from join tables like payload_locked_documents_rels. Cleanup must be done manually with sqlite3 CLI. Key gotcha: ALTER TABLE ... DROP COLUMN fails with \"unknown column in foreign key definition\" when the column is used in a FK constraint (even with foreign_keys=OFF), so the table must be recreated (CREATE new, copy data, DROP old, RENAME) and indexes recreated manually. Also: DROP COLUMN requires dropping any dedicated indexes on the column first. Verification: PRAGMA integrity_check, check sqlite_master for orphan tables, PRAGMA table_info on join tables. Backup db first: sqlite3 hct.db '.backup hct.db.bak'.","created_at":"2026-08-17T03:52:15.994Z","tags":"payload,sqlite,collection-removal,drop-column,foreign-keys,migrations"}